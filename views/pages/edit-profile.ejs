<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof title !== "undefined" ? title + " - " : "" %>Minerva Metrics Hub</title>
  
  <!-- TailwindCSS CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
  
  <!-- Font Awesome -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  
  <!-- Google Fonts - Inter -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet" />
  
  <!-- Custom Styles -->
  <link rel="stylesheet" href="/css/style.css">
  
  <style>
    body {
      font-family: "Inter", sans-serif;
    }
    .profile-input {
      @apply w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition-all;
      min-height: 44px;
    }
    .profile-textarea {
      @apply w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent transition-all;
      min-height: 120px;
    }
    .sticky-footer {
      position: fixed;
      bottom: 0;
      left: 0;
      right: 0;
      background: white;
      border-top: 1px solid #e5e7eb;
      box-shadow: 0 -4px 6px -1px rgba(0, 0, 0, 0.1), 0 -2px 4px -1px rgba(0, 0, 0, 0.06);
      z-index: 50;
    }
    .upgrade-btn {
      display: block;
      width: 100%;
      padding: 12px 24px;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #06b6d4 100%);
      color: white;
      font-weight: 600;
      text-align: center;
      border-radius: 0.5rem;
      transition: all 0.3s ease;
      box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    }
    .upgrade-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
    }
  </style>
</head>
<body class="bg-gray-50 min-h-screen flex flex-col">
  
  <!-- Navigation -->
  <%- include("../partials/navbar") %>
  
  <!-- Flash Messages -->
  <%- include("../partials/flash") %>
  
  <!-- Main Content -->
  <main class="flex-grow pb-24">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
      <!-- Header -->
      <div class="mb-8">
        <h1 class="text-4xl font-black text-gray-900 mb-2">
          Edit Agency <span class="text-gradient">Profile</span>
        </h1>
        <p class="text-gray-600">Keep your information accurate to improve visibility and collaboration.</p>
      </div>

      <div class="grid lg:grid-cols-3 gap-8">
        <!-- Main Form -->
        <div class="lg:col-span-2 space-y-6">
          <form action="/profile/edit" method="POST" id="profileForm" class="space-y-6">
            
            <!-- CARD 1: Basic Information -->
            <div class="bg-white rounded-xl shadow-md p-8">
              <h2 class="text-2xl font-bold text-gray-900 mb-6 flex items-center">
                <span class="text-3xl mr-3">👤</span>
                Basic Information
              </h2>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">
                    Agency Name *
                  </label>
                  <input 
                    type="text" 
                    name="agency_name" 
                    required 
                    value="<%= agency.agency_name %>"
                    class="profile-input"
                    placeholder="Your Agency Name"
                  >
                </div>

                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">
                    Contact Name *
                  </label>
                  <input 
                    type="text" 
                    name="contact_name" 
                    required 
                    value="<%= agency.contact_name %>"
                    class="profile-input"
                    placeholder="Your Full Name"
                  >
                </div>

                <div class="md:col-span-2">
                  <label class="block text-sm font-semibold text-gray-700 mb-2">
                    Email Address
                  </label>
                  <input 
                    type="email" 
                    value="<%= agency.email %>" 
                    disabled
                    class="profile-input bg-gray-100 text-gray-500 cursor-not-allowed"
                  >
                  <p class="text-sm text-gray-500 mt-2">
                    <i class="fas fa-lock mr-1"></i>Email cannot be changed
                  </p>
                </div>
              </div>
            </div>

            <!-- CARD 2: Agency Details -->
            <div class="bg-white rounded-xl shadow-md p-8">
              <h2 class="text-2xl font-bold text-gray-900 mb-6 flex items-center">
                <span class="text-3xl mr-3">🏢</span>
                Agency Details
              </h2>
              <div class="space-y-6">
                <!-- Row 1: Website and Location -->
                <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">
                      <i class="fas fa-globe mr-2 text-indigo-600"></i>Website
                    </label>
                    <input 
                      type="url" 
                      name="website" 
                      value="<%= agency.website || '' %>"
                      placeholder="https://youragency.com"
                      class="profile-input"
                    >
                  </div>

                  <div>
                    <label class="block text-sm font-semibold text-gray-700 mb-2">
                      <i class="fas fa-map-marker-alt mr-2 text-red-600"></i>Location
                    </label>
                    <input 
                      type="text" 
                      name="location" 
                      value="<%= agency.location || '' %>"
                      placeholder="City, Country"
                      class="profile-input"
                    >
                  </div>
                </div>

                <!-- Row 2: Description (Full Width) -->
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">
                    <i class="fas fa-align-left mr-2 text-indigo-600"></i>Description
                  </label>
                  <textarea 
                    name="description" 
                    rows="6" 
                    placeholder="Tell us about your agency, your expertise, and what makes you unique..."
                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent"
                  ><%= agency.description || '' %></textarea>
                  <p class="text-sm text-gray-500 mt-2">Provide a detailed overview of your agency's services and strengths</p>
                </div>
              </div>
            </div>

            <!-- CARD 3: Expertise -->
            <div class="bg-white rounded-xl shadow-md p-8">
              <h2 class="text-2xl font-bold text-gray-900 mb-6 flex items-center">
                <span class="text-3xl mr-3">🎯</span>
                Expertise
              </h2>
              <div class="space-y-6">
                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">
                    <i class="fas fa-cogs mr-2 text-indigo-600"></i>Skills
                  </label>
                  <textarea 
                    name="skills" 
                    rows="2"
                    placeholder="SEO, PPC, Social Media Marketing, Content Marketing, Email Marketing"
                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent"
                  ><%= agency.skills || '' %></textarea>
                  <p class="text-sm text-gray-500 mt-2">Comma-separated list of your core services and capabilities</p>
                </div>

                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">
                    <i class="fas fa-chart-line mr-2 text-emerald-600"></i>Platforms
                  </label>
                  <textarea 
                    name="platforms" 
                    rows="2"
                    placeholder="Google Ads, Meta (Facebook & Instagram), TikTok, LinkedIn, Microsoft Ads"
                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent"
                  ><%= agency.platforms || '' %></textarea>
                  <p class="text-sm text-gray-500 mt-2">Advertising platforms you specialize in</p>
                </div>

                <div>
                  <label class="block text-sm font-semibold text-gray-700 mb-2">
                    <i class="fas fa-industry mr-2 text-indigo-600"></i>Verticals
                  </label>
                  <textarea 
                    name="verticals" 
                    rows="2"
                    placeholder="E-commerce, SaaS, Healthcare, Finance, Real Estate, Education"
                    class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent"
                  ><%= agency.verticals || '' %></textarea>
                  <p class="text-sm text-gray-500 mt-2">Industries you have experience working with</p>
                </div>
              </div>
            </div>

            <!-- CARD 4: Certifications -->
            <div class="bg-white rounded-xl shadow-md p-8">
              <h2 class="text-2xl font-bold text-gray-900 mb-6 flex items-center">
                <span class="text-3xl mr-3">🎓</span>
                Certifications
              </h2>
              <p class="text-gray-600 mb-6">Select any certifications your agency holds. This helps build trust with potential collaborators.</p>
              
              <% const certificationOptions = [
                'Google Partner',
                'Meta Certified Professional',
                'Microsoft Advertising Partner',
                'TikTok Marketing Partner',
                'LinkedIn Marketing Partner',
                'HubSpot Agency Partner',
                'Shopify Partner',
                'Amazon Ads Certified',
                'DV360 / GMP Certified',
                'IAB Certifications'
              ];
              const agencyCerts = agency.certifications ? JSON.parse(agency.certifications) : []; %>
              
              <div class="grid grid-cols-1 md:grid-cols-2 gap-y-4 gap-x-8 mb-6">
                <% certificationOptions.forEach(cert => { %>
                  <label class="flex items-center cursor-pointer group">
                    <input 
                      type="checkbox" 
                      name="certifications" 
                      value="<%= cert %>"
                      <%= agencyCerts.includes(cert) ? 'checked' : '' %>
                      class="w-5 h-5 text-indigo-600 border-gray-300 rounded focus:ring-indigo-500 transition-all"
                    >
                    <span class="ml-3 text-gray-700 font-medium group-hover:text-indigo-600 transition-colors"><%= cert %></span>
                  </label>
                <% }); %>
              </div>
              
              <div class="mt-6">
                <label class="block text-sm font-semibold text-gray-700 mb-2">
                  <i class="fas fa-plus-circle mr-2 text-indigo-600"></i>Other Certifications
                </label>
                <textarea 
                  name="other_certifications" 
                  rows="3"
                  placeholder="Add any other certifications not listed above (comma-separated)"
                  class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-indigo-500 focus:border-transparent"
                ><%= (agencyCerts.filter(c => !certificationOptions.includes(c))).join(', ') || '' %></textarea>
                <p class="text-sm text-gray-500 mt-2">List additional certifications that aren't included above</p>
              </div>
            </div>

          </form>
        </div>

        <!-- Sidebar -->
        <div class="lg:col-span-1 space-y-6">
          <!-- Subscription Card -->
          <div class="bg-white rounded-xl shadow-md p-6 sticky top-24">
            <h3 class="text-xl font-bold text-gray-900 mb-4 flex items-center">
              <i class="fas fa-crown mr-2 text-amber-500"></i>Subscription
            </h3>
            
            <div class="mb-6">
              <div class="flex items-center justify-between mb-2">
                <span class="text-gray-600">Current Plan</span>
                <span class="font-bold text-xl capitalize
                  <%= agency.subscription_tier === 'premium' ? 'badge badge-premium' : '' %>
                  <%= agency.subscription_tier === 'pro' ? 'badge badge-primary' : '' %>
                  <%= agency.subscription_tier === 'free' ? 'badge badge-secondary' : '' %>
                ">
                  <%= agency.subscription_tier %>
                </span>
              </div>
              
              <% if (agency.subscription_tier === 'free') { %>
                <div class="mt-4 p-4 bg-gradient-to-r from-purple-50 to-cyan-50 rounded-lg border border-purple-200">
                  <p class="text-sm text-gray-700 mb-3">
                    <strong>Monthly Limits:</strong>
                  </p>
                  <ul class="text-sm text-gray-600 space-y-2">
                    <li class="flex items-center">
                      <i class="fas fa-project-diagram mr-2 text-indigo-600"></i>
                      <%= 1 - agency.projects_created_this_month %> project(s) remaining
                    </li>
                    <li class="flex items-center">
                      <i class="fas fa-reply mr-2 text-emerald-600"></i>
                      <%= 5 - agency.responses_sent_this_month %> response(s) remaining
                    </li>
                  </ul>
                </div>
                
                <a href="/subscriptions/checkout/pro" class="upgrade-btn mt-6">
                  <i class="fas fa-rocket mr-2"></i>Upgrade to Pro
                </a>
              <% } else if (agency.subscription_tier === 'pro') { %>
                <div class="mt-4 p-4 bg-gradient-to-r from-blue-50 to-indigo-50 rounded-lg border border-blue-200">
                  <p class="text-sm text-gray-700 mb-2">
                    <i class="fas fa-check-circle mr-2 text-green-600"></i>
                    <strong>Unlimited projects & responses</strong>
                  </p>
                  <p class="text-sm text-gray-600">
                    Enhanced profile visibility
                  </p>
                </div>
                
                <a href="/subscriptions/checkout/premium" class="upgrade-btn mt-6">
                  <i class="fas fa-crown mr-2"></i>Upgrade to Premium
                </a>
              <% } else if (agency.subscription_tier === 'premium') { %>
                <div class="mt-4 p-4 bg-gradient-to-r from-amber-50 to-yellow-50 rounded-lg border border-amber-200">
                  <p class="text-sm text-gray-700 mb-2">
                    <i class="fas fa-star mr-2 text-amber-600"></i>
                    <strong>Elite Partner Status</strong>
                  </p>
                  <ul class="text-sm text-gray-600 space-y-1">
                    <li>✓ Unlimited everything</li>
                    <li>✓ Premium badge</li>
                    <li>✓ Top listing priority</li>
                  </ul>
                </div>
              <% } %>
            </div>

            <div class="border-t border-gray-200 pt-4">
              <h4 class="font-semibold text-gray-900 mb-3 flex items-center">
                <i class="fas fa-lightbulb mr-2 text-blue-500"></i>Profile Tips
              </h4>
              <ul class="text-sm text-gray-600 space-y-2">
                <li class="flex items-start">
                  <i class="fas fa-check mr-2 text-green-600 mt-1"></i>
                  <span>Complete all sections to maximize visibility</span>
                </li>
                <li class="flex items-start">
                  <i class="fas fa-check mr-2 text-green-600 mt-1"></i>
                  <span>Add certifications to build trust</span>
                </li>
                <li class="flex items-start">
                  <i class="fas fa-check mr-2 text-green-600 mt-1"></i>
                  <span>Update your expertise regularly</span>
                </li>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>

  <!-- Sticky Footer -->
  <div class="sticky-footer">
    <div class="max-w-7xl mx-auto px-6 py-4">
      <div class="flex items-center justify-end gap-4">
        <a 
          href="/dashboard" 
          class="px-6 py-3 text-gray-700 font-semibold hover:bg-gray-100 rounded-lg transition-all"
        >
          Cancel
        </a>
        <button 
          type="submit" 
          form="profileForm"
          class="px-8 py-3 text-white font-semibold rounded-lg transition-all hover:shadow-lg hover:-translate-y-0.5"
          style="background: linear-gradient(135deg, #667eea 0%, #764ba2 50%, #06b6d4 100%);"
        >
          <i class="fas fa-save mr-2"></i>Save Changes
        </button>
      </div>
    </div>
  </div>
  
  <!-- Footer -->
  <%- include("../partials/footer") %>
  
</body>
</html>
